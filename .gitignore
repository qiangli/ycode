# Binaries
/ycode
bin/
*.exe
*.dll
*.so
*.dylib

# Test binary
*.test

# Embedded inference runner — platform-specific build artifact produced by
# scripts/build-runner-thin.sh. Devs build locally when they want the
# embed_runner build tag; default builds use embed_stub.go and don't need it.
internal/inference/runner_embed/ycode-runner.gz

# Go build cache
/vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Debug
*.log
debug/

.env
.env.*

tmp/
priorart/
reference/
peers/
go.work.local
evals/results/
#
.gfy-out/
#
.agents/


# ycode local artifacts
.agents/ycode.json
.agents/ycode/settings.local.json
.agents/ycode/sessions/
.agents/ycode/cache/
.agents/ycode/logs/
.agents/ycode/foreman/

# Backlog kill-switch sentinel (runtime; backlog/*.md ARE tracked)
docs/backlog/PAUSE

# Claude Code per-project state — permission allowlist, scheduled-task
# locks, worktree state. Per-machine, contains absolute paths, not source.
.claude/
