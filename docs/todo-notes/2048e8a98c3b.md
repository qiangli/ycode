Story #40 implementation note

The story implementation is complete, but the literal gate command
`bashy dag build` is not green in this workspace: it exits 1 immediately before
recording a DAG run or running any target body, and prints no target output.
`bashy dag --check --json` validates `DAG.md`, and `bashy dag --list --json`
lists the `build` target and its dependencies, so the DAG file itself is valid.

The underlying `build` target body and dependencies were run directly and
passed:

- `./scripts/fmtcheck.sh`
- `go vet ./...`
- `go test -count=1 ./internal/features/...`
- `go build -trimpath ... -o bin/ycode ./cmd/ycode/`
- `go test -short -race -count=1 ./...`

The required focused gate before this note also passed:

- `go test -short -race ./internal/harness/... ./pkg/ycode`
- `go vet ./internal/harness/...`
